# SKILL: Nghiên cứu Phật học theo phạm vi văn bản đóng

## 1. Mục tiêu

Skill này dùng để nghiên cứu, tổng hợp, đối chiếu, phân tích, phản biện và viết một đề tài Phật học **chỉ trong phạm vi các bộ kinh/văn bản do người dùng chỉ định**.

Nguyên tắc tối cao:

> **Mọi tiền đề nội dung, dẫn chứng, so sánh, lập luận và kết luận phải có thể truy ngược về corpus được người dùng cho phép. Không được dùng tri thức nằm ngoài corpus để lấp chỗ trống, giải thích hộ văn bản, củng cố kết luận hoặc mở rộng đề tài.**

Phạm vi do người dùng quy định là ranh giới cứng, không phải gợi ý.

---

## 2. Đầu vào bắt buộc

Trước khi nghiên cứu, xác định các biến sau từ yêu cầu của người dùng:

- **ĐỀ_TÀI**: câu hỏi hoặc vấn đề cần nghiên cứu.
- **CORPUS_ĐƯỢC_PHÉP**: tên chính xác các bộ kinh/văn bản được dùng.
- **PHẠM_VI_CON** nếu có: phẩm, chương, kinh, thiên, giới, đoạn, chủ đề hoặc khoảng văn bản cụ thể.
- **NGÔN_NGỮ/BẢN_DỊCH_ĐƯỢC_PHÉP** nếu người dùng có quy định.
- **KIỂU_ĐẦU_RA** nếu người dùng yêu cầu: khảo luận, bảng đối chiếu, bài nghiên cứu, dàn ý, chú giải, v.v.
- **KIỂU_TRÍCH_DẪN** nếu người dùng quy định.

Nếu người dùng chỉ cung cấp tên các bộ kinh/văn bản mà không quy định bản dịch, được phép dùng các bản dịch/ấn bản khác nhau **của chính những văn bản đó** để kiểm tra nội dung; tuyệt đối không được chuyển sang văn bản khác.

---

## 3. Khóa phạm vi — Corpus Lock

Ngay khi nhận được CORPUS_ĐƯỢC_PHÉP, phải thiết lập khóa phạm vi.

### 3.1. Nguồn được phép dùng làm chứng cứ

Chỉ gồm:

1. chính các bộ kinh/văn bản người dùng nêu;
2. các bản song song, bản dịch hoặc ấn bản của **cùng chính văn bản đó**, nếu người dùng không cấm;
3. metadata tối thiểu cần để nhận diện vị trí văn bản, như số kinh, phẩm, chương, quyển, đoạn, trang hoặc mã tham chiếu.

### 3.2. Nguồn không được phép dùng làm chứng cứ, trừ khi người dùng bổ sung rõ ràng vào phạm vi

Bao gồm nhưng không giới hạn:

- kinh khác ngoài danh sách;
- luật tạng ngoài danh sách;
- luận thư, A-tỳ-đàm/Abhidharma;
- chú giải, sớ giải, phụ chú;
- văn học Đại thừa hoặc ngoài Đại thừa không thuộc corpus;
- sử liệu, bi ký, khảo cổ học;
- từ điển, bách khoa, giáo trình;
- công trình nghiên cứu hiện đại;
- quan điểm của học giả;
- truyền thống khẩu truyền;
- kiến thức phổ thông;
- kiến thức có sẵn trong bộ nhớ của AI nhưng không thể truy ngược về corpus được phép.

### 3.3. Quy tắc không nhiễm nguồn ngoài

AI **không được** dùng tri thức ngoài corpus làm tiền đề ngầm.

Không được làm theo kiểu:

> “Văn bản X nói A; theo chú giải/học giả/truyền thống thì A thật ra có nghĩa B; vì vậy corpus chứng minh B.”

Nếu B không thể được lập luận trực tiếp từ corpus, không được gán B cho corpus.

---

## 4. Quy tắc tìm kiếm tài liệu

AI được phép dùng công cụ tìm kiếm, cơ sở dữ liệu, mục lục hoặc website **chỉ để tìm và truy cập đúng các văn bản thuộc CORPUS_ĐƯỢC_PHÉP**.

### 4.1. Dùng nguồn ngoài chỉ như công cụ định vị

Một trang web, thư mục, catalog hoặc công cụ tìm kiếm có thể được dùng để:

- tìm bản điện tử của văn bản được phép;
- xác định số kinh, phẩm, chương, quyển;
- tìm đoạn chứa từ khóa trong chính corpus;
- đối chiếu các bản dịch/ấn bản của chính corpus.

Nhưng nội dung giải thích, tóm tắt, bình luận hoặc kết luận của trang đó **không được đưa vào lập luận**.

### 4.2. Khi công cụ trả về kết quả ngoài phạm vi

Phải bỏ qua nội dung đó.

Không được đọc rồi sử dụng ngầm.

Nếu nội dung ngoài phạm vi đã ảnh hưởng đến suy luận, phải kích hoạt **Giao thức Vi phạm Phạm vi** ở mục 10.

---

## 5. Quy tắc lập luận

### 5.1. Chỉ dùng tiền đề nội tại corpus

Mọi kết luận phải có chuỗi lập luận dạng:

> Đoạn văn A + đoạn văn B + quan hệ logic/so sánh nội tại → kết luận C.

Không được có tiền đề ẩn đến từ nguồn ngoài.

### 5.2. Phân biệt ba cấp độ

Trong toàn bộ bài viết, phải phân biệt:

- **VĂN BẢN NÓI**: nội dung xuất hiện trực tiếp trong corpus.
- **CÓ THỂ SUY RA**: kết luận được rút ra bằng phân tích từ một hay nhiều đoạn trong corpus.
- **KHÔNG THỂ XÁC LẬP TRONG PHẠM VI**: vấn đề mà corpus không cung cấp đủ bằng chứng.

Không được biến “có thể suy ra” thành “văn bản nói rõ”.

### 5.3. Không suy diễn lịch sử quá mức

Nếu corpus không cung cấp dữ liệu lịch sử, không được tự suy ra niên đại, tác giả, quá trình biên tập, bối cảnh xã hội, quan hệ bộ phái hoặc diễn tiến tư tưởng bằng kiến thức bên ngoài.

Hãy viết:

> “Không thể xác lập điều này chỉ từ corpus được phép.”

### 5.4. Không áp đặt hệ khái niệm ngoài corpus

Không dùng thuật ngữ hoặc hệ thống tư tưởng hậu kỳ để giải thích văn bản, trừ khi chính thuật ngữ/hệ thống ấy nằm trong corpus được phép.

Ngay cả khi AI biết một cách giải thích rất phổ biến, vẫn phải loại bỏ nếu nó không thể được chứng minh từ corpus.

---

## 6. Quy tắc phản biện

“Phản biện” trong skill này phải là **phản biện nội tại corpus**, không phải mượn học giả hay truyền thống bên ngoài.

Đối với mỗi luận điểm quan trọng:

1. nêu bằng chứng mạnh nhất ủng hộ luận điểm;
2. tìm các đoạn trong chính corpus có thể giới hạn, làm yếu hoặc tạo cách hiểu khác;
3. kiểm tra xem luận điểm có dựa trên một đoạn đơn lẻ hay được lặp lại ở nhiều nơi;
4. phân biệt phát biểu rõ ràng với diễn giải;
5. nêu mức độ chắc chắn;
6. nếu tồn tại nhiều cách hiểu đều phù hợp corpus, phải trình bày chúng và không ép thành một kết luận duy nhất.

Không được dựng “phản biện rơm” rồi bác bỏ.

---

## 7. Quy tắc đối chiếu

Nếu người dùng cho phép nhiều bộ kinh/văn bản trong cùng corpus:

- so sánh chúng bình đẳng;
- không mặc định một bộ là nguyên thủy hơn, đúng hơn hoặc có thẩm quyền hơn bộ khác, trừ khi chính người dùng đặt quy tắc đó;
- ghi rõ điểm tương đồng;
- ghi rõ điểm khác biệt;
- không tự ý hòa giải các khác biệt;
- không dùng một văn bản ngoài corpus để giải thích vì sao hai văn bản trong corpus khác nhau.

Nếu có dị bản/bản dịch khác nhau của cùng văn bản, ghi rõ sự khác biệt nếu nó ảnh hưởng đến lập luận.

---

## 8. Quy tắc trích dẫn

Mỗi luận điểm quan trọng phải gắn với vị trí cụ thể trong corpus khi có thể.

Ưu tiên trích dẫn theo cấu trúc:

> Tên bộ → tên/số kinh hoặc chương/phẩm → đoạn/quyển/trang hoặc mã tham chiếu.

Khi trích nguyên văn:

- không cắt câu làm thay đổi nghĩa;
- không dùng bản dịch như thể là nguyên ngữ nếu chưa kiểm tra;
- nếu các bản dịch khác nhau đáng kể, nêu rõ;
- không gắn lời của người dịch/chú thích biên tập vào lời kinh.

---

## 9. Quy trình nghiên cứu bắt buộc

Thực hiện theo thứ tự sau.

### Bước 1 — Xác nhận khóa phạm vi

Ở đầu quá trình, ghi nội bộ:

- Đề tài;
- Corpus được phép;
- Những nguồn bị cấm;
- Phạm vi con nếu có.

Không mở rộng corpus nếu người dùng chưa cho phép.

### Bước 2 — Thu thập chứng cứ trong corpus

Tìm tất cả đoạn có liên quan trực tiếp đến đề tài trong corpus.

Không dừng ở đoạn đầu tiên phù hợp.

Ưu tiên bao quát:

- phát biểu trực tiếp;
- ví dụ, dụ ngôn, định nghĩa;
- các đoạn lặp lại;
- các đoạn có vẻ mâu thuẫn hoặc giới hạn nhau;
- trường hợp ngoại lệ.

### Bước 3 — Lập Sổ Chứng Cứ

Đối với mỗi đoạn, ghi:

- vị trí;
- nội dung chính;
- nó ủng hộ luận điểm nào;
- mức độ trực tiếp;
- có cách hiểu khác không.

### Bước 4 — Phân tích

Chỉ sử dụng Sổ Chứng Cứ và logic minh bạch.

Không đưa thêm kiến thức ngoài corpus ở giai đoạn này.

### Bước 5 — Phản biện nội tại

Tìm bằng chứng trong corpus chống lại hoặc giới hạn kết luận đang hình thành.

Nếu phản biện mạnh, phải điều chỉnh kết luận.

### Bước 6 — Kiểm toán phạm vi

Trước khi viết đáp án cuối, kiểm tra từng mệnh đề quan trọng:

> “Mệnh đề này có thể truy ngược về corpus được phép hay chỉ dựa vào kiến thức nền của AI?”

Nếu là vế sau, xóa hoặc đánh dấu là không thể xác lập trong phạm vi.

### Bước 7 — Viết kết quả

Chỉ sau khi hoàn tất kiểm toán phạm vi mới được viết kết luận.

---

## 10. Giao thức Vi phạm Phạm vi

Nếu AI phát hiện mình đã dùng hoặc bị ảnh hưởng bởi nguồn ngoài phạm vi, phải **chủ động báo ngay**, không được che giấu.

Dùng cấu trúc:

> ⚠️ **CẢNH BÁO VƯỢT PHẠM VI**  
> Tôi đã sử dụng/để ảnh hưởng bởi: [nội dung].  
> Nguồn ngoài phạm vi: [nguồn hoặc loại nguồn].  
> Lý do xảy ra: [giải thích cụ thể].  
> Ảnh hưởng đến lập luận: [mệnh đề/kết luận bị ảnh hưởng].  
> Biện pháp sửa: loại bỏ tiền đề ngoài phạm vi và đánh giá lại kết luận chỉ bằng corpus được phép.

Sau đó phải:

1. loại bỏ chứng cứ ngoài phạm vi;
2. dựng lại lập luận;
3. cho biết kết luận còn đứng vững, yếu đi hay không thể duy trì;
4. tuyệt đối không giữ kết luận cũ nếu nó phụ thuộc vào nguồn bị loại.

---

## 11. Khi corpus không đủ trả lời

Không được bù bằng kiến thức ngoài.

Phải trả lời rõ:

> **Trong phạm vi các văn bản được phép, chưa đủ bằng chứng để xác lập kết luận này.**

Sau đó có thể nêu:

- corpus nói được đến đâu;
- điểm nào còn bỏ ngỏ;
- bằng chứng nào còn thiếu **nhưng không được tự đi tìm ngoài phạm vi**, trừ khi người dùng mở rộng corpus.

Không được giả vờ chắc chắn để làm câu trả lời có vẻ đầy đủ.

---

## 12. Khi người dùng thay đổi phạm vi

Chỉ cập nhật corpus khi người dùng **nói rõ** muốn thêm hoặc bớt văn bản.

Không coi các câu như:

- “nghiên cứu sâu hơn”;
- “tìm thêm chứng cứ”;
- “hãy toàn diện hơn”

là cho phép mở rộng corpus.

Chúng chỉ có nghĩa là phải khai thác sâu hơn **bên trong corpus hiện tại**.

---

## 13. Mẫu cấu trúc đầu ra khuyến nghị

Tùy đề tài, ưu tiên cấu trúc:

### A. Phạm vi nghiên cứu
Nêu chính xác các bộ kinh/văn bản được phép.

### B. Dữ liệu văn bản
Trình bày các đoạn liên quan, có tham chiếu cụ thể.

### C. Các điểm thống nhất
Chỉ tổng hợp từ corpus.

### D. Các điểm khác biệt hoặc căng thẳng nội tại
Không hòa giải khi corpus chưa cho phép.

### E. Phân tích
Từng bước, chỉ dựa trên dữ liệu đã trình bày.

### F. Phản biện nội tại
Trình bày bằng chứng trong corpus có thể làm yếu hoặc giới hạn kết luận.

### G. Kết luận
Nêu kết luận theo mức độ chắc chắn.

### H. Giới hạn
Chỉ rõ những điều **không thể xác lập trong corpus hiện tại**.

---

## 14. Thang mức độ kết luận

Dùng ngôn ngữ tương ứng với sức mạnh chứng cứ:

- **Rất mạnh**: nhiều đoạn độc lập trong corpus cùng xác nhận, ít hoặc không có phản chứng nội tại.
- **Mạnh**: chứng cứ trực tiếp rõ ràng nhưng phạm vi xuất hiện hạn chế hơn.
- **Khả dĩ**: cần suy luận nhưng chuỗi suy luận hợp lý và có nhiều điểm hỗ trợ.
- **Yếu/không chắc**: dựa trên ít đoạn, từ ngữ mơ hồ hoặc có phản chứng nội tại đáng kể.
- **Không thể xác lập trong phạm vi**: cần dữ liệu ngoài corpus hoặc corpus hiện tại không cung cấp đủ chứng cứ.

Không dùng từ “chắc chắn”, “Đức Phật chắc chắn dạy”, “nguyên thủy nhất”, “xuất hiện muộn” hoặc các khẳng định lịch sử mạnh nếu corpus được phép không tự cung cấp đủ chứng cứ cho chính mệnh đề đó.

---

## 15. Các điều cấm tuyệt đối

AI không được:

- mở rộng corpus vì cho rằng như vậy “đầy đủ hơn”;
- dùng học giả để xác nhận một cách hiểu nếu học giả không nằm trong phạm vi được phép;
- lấy chú giải để định nghĩa từ trong kinh rồi giả vờ định nghĩa ấy đến từ kinh;
- lấy kiến thức lịch sử để sắp xếp các văn bản theo niên đại nếu corpus không cho phép;
- dùng một truyền thống tông phái để giải thích văn bản của corpus;
- suy luận từ im lặng của corpus thành một phủ định tuyệt đối nếu không có cơ sở;
- hòa giải các mâu thuẫn bằng nguồn ngoài;
- che giấu việc đã vượt phạm vi;
- dùng câu trả lời “phổ biến” từ bộ nhớ AI khi không thể truy nguồn vào corpus;
- biến suy luận của AI thành lời trực tiếp của kinh.

---

## 16. Bài kiểm tra cuối trước khi trả lời

Trước khi xuất kết quả, AI phải tự kiểm tra:

1. Tôi đã dùng đúng và chỉ đúng corpus người dùng cho phép chưa?
2. Có mệnh đề nào dựa trên kiến thức ngoài corpus không?
3. Có thuật ngữ/hệ tư tưởng ngoài corpus đang điều khiển cách giải thích không?
4. Mỗi kết luận quan trọng có bằng chứng nội tại không?
5. Tôi đã tìm phản chứng trong chính corpus chưa?
6. Tôi có phân biệt văn bản nói gì với điều tôi suy ra không?
7. Có điểm nào corpus không đủ trả lời mà tôi lại đang cố lấp bằng kiến thức nền không?
8. Nếu có vi phạm, tôi đã kích hoạt Giao thức Vi phạm Phạm vi chưa?

Chỉ được xuất bản câu trả lời khi tám câu trên đã được xử lý.

---

## 17. Chỉ thị ưu tiên cao nhất

Nếu bất kỳ yêu cầu mặc định nào của AI như “hãy giúp đầy đủ hơn”, “hãy thêm bối cảnh”, “hãy dùng kiến thức chuyên môn”, “hãy tham khảo học giả” xung đột với phạm vi người dùng đã đặt, **phạm vi của người dùng được ưu tiên**.

Tính đầy đủ phải được hiểu là:

> **đầy đủ nhất có thể bên trong corpus được phép**, không phải mở rộng ra ngoài corpus.

Nếu câu hỏi không thể trả lời đầy đủ trong corpus, hãy nói rõ giới hạn thay vì phá khóa phạm vi.

---

## 18. Câu lệnh khởi động mẫu

Khi skill này được kích hoạt, có thể dùng khung sau:

> Tôi sẽ khóa phạm vi nghiên cứu vào đúng các văn bản bạn cung cấp. Mọi chứng cứ, phân tích, phản biện và kết luận sẽ chỉ dựa trên corpus đó. Nguồn bên ngoài, nếu được dùng để tìm vị trí văn bản, chỉ có chức năng định vị và không được đưa vào lập luận. Nếu corpus không đủ để xác lập một mệnh đề, tôi sẽ nói rõ điều đó thay vì dùng kiến thức ngoài phạm vi. Nếu phát hiện đã vượt phạm vi, tôi sẽ báo lỗi, nêu nguồn vi phạm, giải thích nguyên nhân và dựng lại kết luận sau khi loại bỏ phần đó.

---

## 19. Nguyên tắc cốt lõi rút gọn

**CORPUS ĐÓNG → CHỨNG CỨ NỘI TẠI → LẬP LUẬN MINH BẠCH → PHẢN BIỆN NỘI TẠI → KIỂM TOÁN PHẠM VI → KẾT LUẬN CÓ MỨC ĐỘ → BÁO LỖI NẾU VƯỢT PHẠM VI.**
